- Primeiramente, faz-se a instalação das seguintes bibliotecas:
pip install selenium
pip install webdriver_manager
pip install ipython
pip install pandas

- Em seguida, adiciono o arquivo chromedriver.exe no mesmo diretório que meu arquivo main.py.
